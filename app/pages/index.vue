<script setup lang="ts">
// Ví dụ dùng useApi — bỏ comment khi backend đã sẵn sàng.
// const api = useApi()
// const { data: users } = await useAsyncData('users', () => api.users.list())
</script>

<template>
  <section>
    <h1>Nuxt 4 Base</h1>
    <p>Base dự án đã sẵn sàng. Xem <code>app/composables/useApi.ts</code> để gọi API.</p>
  </section>
</template>
